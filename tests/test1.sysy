int counter = 3;

int change() {
    counter = counter - 1;
    return counter;
}

int main() {
    int a = 0;
    int b = 1;
    int c = 1;

    if (a && change() > 0 || b && c) {
        return counter;
    }
    return 0;
}//返回3
