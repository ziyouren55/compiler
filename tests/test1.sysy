// 示例1：返回数组而非 int
void dfg(){
  int a;
  return dfg;
}

int returnArray() {
    int arr[3] = {1, 2, 3};
    int a = 1;
    if(dfg >= a)
    {
        int b = 1;
    }
    // 错误：函数声明为返回 int，但返回了一个数组
    return a;  // Error type 7: Return type mismatch in function: returnArray
}

