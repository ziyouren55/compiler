int mul(int a, int b) {
    return a * b;
}

int add(int a, int b) {
    return a + b;
}

int main() {
    int result = mul(add(2, 3), add(4, 5)); // add(2,3) 和 add(4,5) 均返回 int，传入 mul 中匹配正确
    return result;
}
