int process(int arr[3]) { return arr[0]; }
int main() {
    int matrix[2][3];
    // 将二维数组中的一维子数组传入是合法的，但如果传入整个二维数组则会出错：
    process(matrix);       // 错误：matrix 是二维数组，不是一维数组
    process(matrix[0]);      // 正确：matrix[0] 是一维数组
    return 0;
}
