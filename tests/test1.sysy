int main() {
    int a[2];
    // 数组不能参与算术运算
    int b = a + 1;
    int c = 2 * a;
    int d = a / 2;

    // 数组不能参与比较运算
    if(a > 1) {
        return 0;
    }

    // 数组不能参与逻辑运算
    while(a && true) {
        break;
    }

    return 0;
}
