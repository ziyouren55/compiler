void main() {
    int i = 0;
    while (i < 10) {
        if (i % 2 == 0) {
            x = x + i;
        } else {
            y[i] = x * i;
        }
        i = i + 1;
    }
    return;
}
const int a = 10;const int b = 20;const int arr[2] = {1, 2};int x = 5;int y[10];int z = {1, 2, 3};
int sum(int x, int y) {
    return x + y;
}
void test() {
    int result = sum(a, b);
    if (result > 10) {
        result = result - 10;
    } else if (result < 20) {
        result = result + 10;
    }
    return;
}
int factorial(int n) {
    if (n <= 1) {
        return 1;
    } else {
        return n * factorial(n - 1);
    }
}
void calculate() {
    int num = 5;
    int fact = factorial(num);
    while (count < max) {
        count = count + 1;
    }
    return;
}
